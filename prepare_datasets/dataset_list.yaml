# This file defines all datasets to be used in the benchmark.
# The 'prepare_all_datasets.py' script reads this file to automate preparation.

ROOT_DATA: ./data

datasets:
  - name: for-original
    # The script that will be used to process this dataset.
    script_path: prepare_datasets/prep_fororiginal.py
    args:
      # The location of the raw, unzipped data.
      --source_dir: $ROOT_DATA/for-original/for-original/testing
      # The desired location for the final manifest CSV file.
      --output_path: manifest/for-original.csv

  - name: for-norm
    script_path: prepare_datasets/prep_fornorm.py
    args:
      --source_dir: $ROOT_DATA/for-norm/for-norm/testing
      --output_path: manifest/for-norm.csv

  - name: for-2s
    script_path: prepare_datasets/prep_for2s.py
    args:
      --source_dir: $ROOT_DATA/for-2s/for-2s/testing
      --output_path: manifest/for-2s.csv

  - name: for-rerecorded
    script_path: prepare_datasets/prep_for-rerecorded.py
    args:
      --source_dir: $ROOT_DATA/for-rerecorded/for-rerecorded/testing
      --output_path: manifest/for-rerecorded.csv

  - name: playback-attacks
    script_path: prepare_datasets/prep_playback-attacks.py
    args:
      --source_dir: $ROOT_DATA/playback_attacks/data
      --label_file: $ROOT_DATA/playback_attacks/metadata.csv
      --output_path: manifest/playback-attacks.csv

  # - name: "Another_Dataset_Type"
  #   script_path: "datasets/prepare_another_type.py"
  #   args:
  #     --audio_folder: "/path/to/another/dataset"
  #     --json_metadata: "/path/to/another/metadata.json"
  #     --output_file: "data/manifests/another_type.csv"
  #     --sample_rate: 16000

# Add other datasets here, each with its specific script and arguments.

